# Ignore the backup directory until we figure out how best to handle it
backup/

# As well as any .json files generated by the python scripts
v2x/*.json
modem/*.json
rsu.*.json

**/*.pyc # In addition to the compiled Python binaries
**/*.cur # and the .cur files

# Also all of the configuration files
etc/wpa*
common/xc.conf
common/xcmodem_topology
common/.xcmodem_boardid
